version: '2'
services:
  app:
    environment:
    - PASSWORD=password
    - USER=user
    cap_add:
    - SYS_PTRACE
    image: vdi:xfce
    stdin_open: true
    tty: true
    ports:
    - 4001:4000/tcp
    - 4443:4443/tcp
#    - 4080:4080/tcp

    volumes:
    - ./server.cfg:/usr/NX/etc/server.cfg
    - ./node.cfg:/usr/NX/etc/node.cfg
    # - ./volumes/home:/home
    # networks:
    #   - backend

    # labels:
      # - "traefik.docker.network=backend"
      # - "traefik.domain=vdi.dtpnk.tech"
      # - "traefik.frontend.rule=Host:vdi.dtpnk.tech"
      # - "traefik.enable=true"
      # - "traefik.port=4080"
      # - "traefik.frontend.passHostHeader=true"
      # - "traefik.frontend.whiteList.useXForwardedFor=true"

# networks:
#   backend:
#     external: true
